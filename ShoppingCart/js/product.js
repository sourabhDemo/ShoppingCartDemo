// product class
function product(sku, name, description, price, feature) {
    this.sku = sku; 
    this.name = name;
    this.description = description;
    this.price = price;
    this.feature = feature;
}
