Few point:

1. It uses localstorage, as no server code is there. So every time you run on new computer, two records are shown.

2. Add functionality for feature nis WIP.

3. For image, as of now, name should be same as product name and should be in image folder.